<?xml version="1.0" encoding="utf-8"?>
<!--
  Legacy (API 26–30) equivalent of data_extraction_rules.xml, which only applies from
  API 31. allowBackup is false so nothing should ever be backed up on any API level;
  these exclusions are defence in depth for the same paths (QA-8).
-->
<full-backup-content>
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="modules" />
</full-backup-content>
